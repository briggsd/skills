# CLAUDE.md — Master Index

This file is the root instruction manual for Claude. Read it at the start of every session before taking action in this vault.

## Purpose

This vault is the user's central knowledge and memory hub — a persistent "second brain" that lives across sessions. Anything worth remembering long-term about the user's work, projects, people, and decisions lives here.

Flavor: **{{FLAVOR}}**

## How to use this vault

1. **Read the relevant folder's `CLAUDE.md` before creating or modifying files in it, or before making decisions that affect that folder's content.** Pure reads don't require the protocol.
2. **Read `_guide.md` if you need the human-facing explanation** of how a folder works.
3. **Use templates from `_templates/`** when creating new notes. Don't reinvent structure.
4. **Convert relative dates to absolute dates** (e.g., "next Thursday" → `YYYY-MM-DD`) when writing anything to this vault.

## Folder map

{{FOLDER_MAP}}

## Naming conventions

- Files: `kebab-case.md` unless a folder's `CLAUDE.md` says otherwise.
- Daily notes: `YYYY-MM-DD.md`.
- Project folders: `kebab-case` matching the project name.
- People: `firstname-lastname.md`.

## Link style

This vault uses: **{{LINK_STYLE_NAME}}**

Example: {{LINK_STYLE_EXAMPLE}}

## Maintenance rules

{{MAINTENANCE_RULES}}

## What NOT to put here

- Secrets, API keys, passwords — never.
- Sensitive personal info (SSN, financial account numbers, medical records beyond what the user explicitly wants here).
- Anything the user would not want to read back in six months.

---

*This vault was scaffolded by the `knowledge-vault` skill. Re-invoke the skill for maintenance tasks like auditing, adding folders, or migrating link styles.*
